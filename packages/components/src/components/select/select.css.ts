import { atoms } from '@papyrus-ui/styles';

export const input = atoms({
  position: 'relative',
  zIndex: 10,
});

export const inputSingle = atoms({
  pr: 7,
});

export const inputMultiple = atoms({
  pr: 4,
});

export const endAction = atoms({
  position: 'absolute',
  top: 0,
  right: 0,
  bottom: 0,
  m: 'auto',
});
